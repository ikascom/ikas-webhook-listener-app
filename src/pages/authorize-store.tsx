import React from 'react';
import { AuthorizeStore } from '@ikas-apps/common-client';
import { Button, Input } from '@ikas/components';

export default () => <AuthorizeStore src="/logo.png" Input={Input} Button={Button} />;
